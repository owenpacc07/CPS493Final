import { MaybeRefOrGetter } from 'vue';
/** check if element is visible in browser view port */
export declare function isElementInView(elementRef: MaybeRefOrGetter<HTMLElement>): boolean;
/** check if an element is currently scrollable */
export declare function isScrollable(elementRef: MaybeRefOrGetter<HTMLElement>): boolean;
/**
 * ensure a given child element is within the parent's visible scroll area
 * if the child is not visible, scroll the parent
 */
export declare function maintainScrollVisibility(activeElement: MaybeRefOrGetter<HTMLElement>, scrollParent: MaybeRefOrGetter<HTMLElement>): void;
